saloraxz.com, www.saloraxz.com {

    handle_path /api/console* {
        reverse_proxy localhost:5000
    }

    # 1. Routes Nextcloud traffic to the container name and strips the path
    handle_path /nextcloud* {
        reverse_proxy nextcloud-app:80 {
            header_up Host {http.request.host}
            header_up X-Real-IP {http.request.remote}
            header_up X-Forwarded-For {http.request.remote}
            header_up X-Forwarded-Proto {http.request.scheme}
        }
    }

    # 2. Catch-all block handles your standard files last
    handle {
        root * /srv/repo/current/www
        file_server
    }

    redir /.well-known/carddav /nextcloud/remote.php/dav/ 301
    redir /.well-known/caldav /nextcloud/remote.php/dav/ 301
}

# ntfy Dashboard
ntfy.saloraxz.com:8080 {
    reverse_proxy ntfy:80
}
