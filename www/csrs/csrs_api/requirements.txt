# csrs-web (FastAPI frontend server)
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic>=2.0
python-dotenv==1.0.0

# sortedcontainers: used in main.py's _build_match_date_index for O(log n)
# last-match date lookups (SortedList per team, bisect_left for date queries)
sortedcontainers
